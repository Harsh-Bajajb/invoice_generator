import { defineConfig } from 'prisma/config';

// Runtime connection is managed by the LibSQL adapter in lib/prisma.js.
// This file is only used by the Prisma CLI (e.g. prisma generate).
export default defineConfig({});
